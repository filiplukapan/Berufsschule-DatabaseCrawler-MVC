@model IEnumerable<databaseCrawlerFinal.Models.characters>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.character_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.race_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.character_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.race_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.character_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.character_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.character_id })
        </td>
    </tr>
}

</table>
